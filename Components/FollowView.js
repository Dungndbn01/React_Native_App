import React, { Component } from 'react';
import {
    View,
} from 'react-native';

export default class FollowView extends Component {
    render(){
        return(
            <View style = {{flex: 1}}>
            </View>
        )
    }
}
